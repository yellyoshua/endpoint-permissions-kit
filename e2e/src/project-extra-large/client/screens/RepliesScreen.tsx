import CollectionScreen from '../CollectionScreen';

export default function RepliesScreen() {
  return <CollectionScreen title="Ticket replies" subject="reply" path="/support/tickets/replies" action="xl.support.tickets.replies" name="all" />;
}
