import CollectionScreen from '../CollectionScreen';

export default function ReportsReadOnlyScreen() {
  return <CollectionScreen title="Reports (read-only)" subject="report" path="/analytics/reports/read-only" action="xl.analytics.reports" name="read-only" />;
}
