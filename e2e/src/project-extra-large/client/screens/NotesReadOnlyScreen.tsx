import CollectionScreen from '../CollectionScreen';

export default function NotesReadOnlyScreen() {
  return <CollectionScreen title="Contact notes (read-only)" subject="contact note" path="/crm/accounts/contacts/notes/read-only" action="xl.crm.accounts.contacts.notes" name="read-only" />;
}
