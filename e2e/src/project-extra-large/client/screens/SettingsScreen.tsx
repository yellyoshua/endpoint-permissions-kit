import CollectionScreen from '../CollectionScreen';

export default function SettingsScreen() {
  return <CollectionScreen title="Tenant settings" subject="tenant" path="/platform/tenants/settings" action="xl.platform.tenants.settings" name="all" />;
}
