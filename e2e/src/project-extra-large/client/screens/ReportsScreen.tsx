import CollectionScreen from '../CollectionScreen';

export default function ReportsScreen() {
  return <CollectionScreen title="Reports" subject="report" path="/analytics/reports" action="xl.analytics.reports" name="all" />;
}
