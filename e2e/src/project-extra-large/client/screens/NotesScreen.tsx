import CollectionScreen from '../CollectionScreen';

export default function NotesScreen() {
  return <CollectionScreen title="Contact notes" subject="contact note" path="/crm/accounts/contacts/notes" action="xl.crm.accounts.contacts.notes" name="all" />;
}
